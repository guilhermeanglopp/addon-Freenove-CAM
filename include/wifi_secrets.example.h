#pragma once

/**
 * Credenciais WiFi de exemplo (não use em produção).
 *
 * Para as suas credenciais sem as enviar para o Git:
 *   1. Copie este ficheiro para include/wifi_secrets.h
 *   2. Edite WIFI_SSID e WIFI_PASSWORD em wifi_secrets.h
 *
 * O ficheiro wifi_secrets.h está no .gitignore e não será incluído em git push.
 */
#define WIFI_SSID "SEU_SSID_WIFI"
#define WIFI_PASSWORD "SUA_SENHA_WIFI"
